import 'package:flutter/material.dart';

Column settingsPage() {
  return Column(
    children: const [
      Center(
          child: Text(
        "You're on the Settings page!",
        style: TextStyle(
          color: Colors.blue,
        ),
      ))
    ],
  );
}
